/users/
/data/
*.tar
*.keras
*.H5
*.csv
*.json
# ...except the tutorials that ship WITH the application. They are source -- the
# wording is reviewed like any other text in the interface -- rather than data
# somebody's run produced, which is what the rule above is for.
!py/tutorials/seed/*.json
*.sdf
*.screen
*__pycache__*

# Editor scratch copies: foo.js.bak_before, design.py.bak_prehaplotype.
# NOT plain *.bak -- py/ppsets/models/model.joblib.bak is tracked on purpose.
*.bak_*
*.orig
*.rej
